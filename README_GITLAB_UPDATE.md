# Обновление Team_poker для GitLab

1. Укажите URL в `gitlab-integration-config.js`.
2. Опубликуйте `firestore.rules`.
3. Замените файлы Team_poker из этой папки.
4. Выполните `Cmd + Shift + R`.

При фиксации оценки задание GitLab создаётся атомарно вместе с итоговой оценкой.
