# Team_poker — hotfix отображения задач

Исправлена архитектурная регрессия последнего релиза:
основной render списка задач больше не зависит от панели
массовых действий.

Теперь:
- Firestore tasks сначала всегда отображаются;
- ошибка bulk-selection / bulk-toolbar не скрывает задачи;
- при ошибке расширенного renderer включается простой fallback;
- ошибка самого Firestore listener показывается явно вместо
  вводящего в заблуждение «Нет задач».

Все функции последнего релиза сохранены:
- обновление статусов;
- фильтры GitLab;
- единый массовый выбор;
- массовое голосование;
- массовый перенос с/без переоценки.

Деплой:
- заменить app.js

Firestore Rules, metadata_connector и Team_calculator
в этом hotfix не менялись.
